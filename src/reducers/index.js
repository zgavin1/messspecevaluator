import { combineReducers } from 'redux'

const messSpecs = (state = [], action) => {
   switch (action.type) {
      default: 
         return state;
   }
}

export default combineReducers({
   messSpecs
})